# URLs de SEO Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** dar às páginas públicas URLs e títulos que descrevem o imóvel, e criar as duas páginas de pretensão como caminhos canônicos.

**Architecture:** três helpers puros em `shared/utils/` (slug da URL, caminho completo, título) passam a ser a única fonte dessas strings; a página de detalhe muda de rota e os oito lugares que montavam URL à mão passam a chamar o helper. As páginas de pretensão reusam a rota de categoria que já existe, estendendo `PropertyCategory` para aceitar `type: null`.

**Tech Stack:** Nuxt 4.5, Vue 3, vitest (Node puro, sem ambiente Nuxt), Supabase.

**Spec:** [docs/superpowers/specs/2026-08-25-urls-seo-design.md](../specs/2026-08-25-urls-seo-design.md)

## Global Constraints

- **Estilo por diretório.** `shared/` e `server/` usam aspas simples, sem ponto-e-vírgula. `app/pages/` e `app/components/` já migraram para aspas duplas com ponto-e-vírgula (há `.prettierrc.json` na raiz). Siga o estilo do arquivo que estiver editando; não reformate arquivo alheio.
- **Testes rodam com `npx vitest run`** e importam por alias `~~/` a partir da raiz. Não existe ambiente Nuxt de teste — os helpers precisam ser funções puras, sem auto-import.
- **Nenhuma migration.** O código continua sendo a chave da URL. Nada de coluna nova.
- **Todos os redirects são 301** e emitidos no servidor.
- **Piso de conteúdo:** `CATEGORY_MIN_PROPERTIES = 3`, já definido em `shared/utils/category.ts`.
- **O conteúdo visível não muda.** `<h1>` e o texto do card continuam exibindo `property.title`, escrito pela imobiliária. Só metadado e URL são compostos.

---

### Task 1: Helpers de URL do imóvel

**Files:**
- Create: `shared/utils/property-url.ts`
- Test: `test/shared/property-url.test.ts`

**Interfaces:**
- Consumes: `PropertyType` de `~~/shared/models/property`
- Produces:
  - `slugify(text: string): string`
  - `interface PropertyUrlFields { type: PropertyType; bedrooms: number; neighborhood?: string | null; code: string }`
  - `propertySlug(p: PropertyUrlFields): string`
  - `propertyPath(p: PropertyUrlFields): string`

- [ ] **Step 1: Escreva o teste que falha**

Crie `test/shared/property-url.test.ts`:

```ts
import { describe, expect, test } from 'vitest'
import { slugify, propertySlug, propertyPath } from '~~/shared/utils/property-url'

describe('slugify', () => {
  test('remove acento e baixa a caixa', () => {
    expect(slugify('Três Lagoas')).toBe('tres-lagoas')
    expect(slugify('Jardim Alvorada')).toBe('jardim-alvorada')
  })

  // Os dados reais têm "Vila piloto " com espaço no fim e "V.L DE Leon".
  test('colapsa separadores e não deixa hífen nas pontas', () => {
    expect(slugify('  Vila piloto  ')).toBe('vila-piloto')
    expect(slugify('V.L DE Leon')).toBe('v-l-de-leon')
    expect(slugify('---')).toBe('')
  })
})

describe('propertySlug', () => {
  test('casa usa tipo, quartos e bairro', () => {
    const s = propertySlug({ type: 'casa', bedrooms: 3, neighborhood: 'Centro', code: 'NC-0231' })
    expect(s).toBe('casa-3-quartos-centro')
  })

  // Terreno não tem quartos: sem esta regra sairia "terreno-0-quartos".
  test('terreno omite os quartos', () => {
    const s = propertySlug({ type: 'terreno', bedrooms: 0, neighborhood: 'Village do Lago', code: 'NC-0258' })
    expect(s).toBe('terreno-village-do-lago')
  })

  test('imóvel sem bairro omite o trecho, sem deixar hífen sobrando', () => {
    const s = propertySlug({ type: 'casa', bedrooms: 3, neighborhood: null, code: 'NC-0301' })
    expect(s).toBe('casa-3-quartos')
  })

  test('quartos zero em imóvel construído também é omitido', () => {
    const s = propertySlug({ type: 'apartamento', bedrooms: 0, neighborhood: 'Centro', code: 'X1' })
    expect(s).toBe('apartamento-centro')
  })
})

describe('propertyPath', () => {
  test('monta /{slug}/{codigo}', () => {
    const path = propertyPath({ type: 'casa', bedrooms: 3, neighborhood: 'Centro', code: 'NC-0231' })
    expect(path).toBe('/casa-3-quartos-centro/NC-0231')
  })

  // O código é a CHAVE e vai cru, preservando caixa e pontuação: é o que a
  // consulta usa. Só o que não é seguro em URL é escapado.
  test('preserva o código como está, escapando o que a URL exige', () => {
    expect(propertyPath({ type: 'casa', bedrooms: 2, neighborhood: null, code: 'V.D 005' })).toBe(
      '/casa-2-quartos/V.D%20005',
    )
    expect(propertyPath({ type: 'terreno', bedrooms: 0, neighborhood: null, code: 'V.D0013' })).toBe(
      '/terreno/V.D0013',
    )
  })
})
```

- [ ] **Step 2: Rode para ver falhar**

Run: `npx vitest run test/shared/property-url.test.ts`
Expected: FAIL com `Cannot find module '~~/shared/utils/property-url'`

- [ ] **Step 3: Implemente o mínimo**

Crie `shared/utils/property-url.ts`:

```ts
import type { PropertyType } from '~~/shared/models/property'

/**
 * URL pública do imóvel: /{slug-descritivo}/{codigo}.
 *
 * O primeiro segmento é decorativo — derivado de tipo/quartos/bairro, ele muda
 * quando o imóvel é editado. O segundo é a chave e é o único que a resolução
 * consulta, então editar o cadastro não quebra link: redireciona.
 *
 * Vive em shared/ porque o card, o canonical, o sitemap, o feed XML e o
 * markdown para agentes precisam produzir exatamente a mesma string. Quando
 * isso era montado à mão em oito lugares, divergir era questão de tempo.
 */
export interface PropertyUrlFields {
  type: PropertyType
  bedrooms: number
  neighborhood?: string | null
  code: string
}

/** Texto livre -> segmento de URL: sem acento, minúsculo, hífen entre palavras. */
export function slugify(text: string): string {
  return text
    .normalize('NFD')
    .replace(/[̀-ͯ]/g, '')
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '')
}

/** Segmento descritivo. Trecho ausente some inteiro, sem deixar hífen duplo. */
export function propertySlug(p: PropertyUrlFields): string {
  // Terreno não tem quartos; imóvel construído com 0 também não deve anunciar.
  const quartos = p.type !== 'terreno' && p.bedrooms > 0 ? `${p.bedrooms}-quartos` : ''
  return [p.type, quartos, p.neighborhood ? slugify(p.neighborhood) : ''].filter(Boolean).join('-')
}

/** Caminho completo. O código vai cru: é a chave que a consulta usa. */
export function propertyPath(p: PropertyUrlFields): string {
  return `/${propertySlug(p)}/${encodeURIComponent(p.code)}`
}
```

- [ ] **Step 4: Rode para ver passar**

Run: `npx vitest run test/shared/property-url.test.ts`
Expected: PASS, 8 testes

- [ ] **Step 5: Commit**

```bash
git add shared/utils/property-url.ts test/shared/property-url.test.ts
git commit -m "feat(seo): helper de URL do imóvel com slug descritivo"
```

---

### Task 2: Helper de título do imóvel

**Files:**
- Create: `shared/utils/property-title.ts`
- Test: `test/shared/property-title.test.ts`

**Interfaces:**
- Consumes: `PropertyType`, `PropertyPurpose`, `PROPERTY_TYPE_LABELS` de `~~/shared/models/property`
- Produces:
  - `interface PropertyTitleFields { type: PropertyType; purpose: PropertyPurpose; bedrooms: number; area: number; neighborhood?: string | null; city?: string | null }`
  - `propertyTitle(p: PropertyTitleFields): string`

- [ ] **Step 1: Escreva o teste que falha**

Crie `test/shared/property-title.test.ts`:

```ts
import { describe, expect, test } from 'vitest'
import { propertyTitle } from '~~/shared/utils/property-title'

const base = {
  type: 'casa' as const,
  purpose: 'venda' as const,
  bedrooms: 3,
  area: 180,
  neighborhood: 'Centro',
  city: 'Três Lagoas',
}

describe('propertyTitle', () => {
  test('compõe tipo, quartos, pretensão, bairro e cidade', () => {
    expect(propertyTitle(base)).toBe('Casa 3 quartos à venda no Centro, Três Lagoas')
  })

  test('aluguel troca a pretensão', () => {
    expect(propertyTitle({ ...base, purpose: 'aluguel' })).toBe(
      'Casa 3 quartos para alugar no Centro, Três Lagoas',
    )
  })

  // Hoje sairia "1 quartos".
  test('um quarto é singular', () => {
    expect(propertyTitle({ ...base, bedrooms: 1 })).toBe(
      'Casa 1 quarto à venda no Centro, Três Lagoas',
    )
  })

  // Terreno não tem quartos. Sem a área, dois terrenos do mesmo bairro
  // produziriam títulos idênticos — foi o que a simulação com dado real mostrou.
  test('terreno usa área no lugar dos quartos', () => {
    expect(propertyTitle({ ...base, type: 'terreno', bedrooms: 0, area: 300 })).toBe(
      'Terreno 300m² à venda no Centro, Três Lagoas',
    )
  })

  test('terreno sem área cadastrada omite a medida', () => {
    expect(propertyTitle({ ...base, type: 'terreno', bedrooms: 0, area: 0 })).toBe(
      'Terreno à venda no Centro, Três Lagoas',
    )
  })

  test('área fracionada é arredondada', () => {
    expect(propertyTitle({ ...base, type: 'terreno', bedrooms: 0, area: 299.6 })).toBe(
      'Terreno 300m² à venda no Centro, Três Lagoas',
    )
  })

  // Dado real: "Vila piloto " chega com espaço no fim.
  test('limpa espaço sobrando do bairro', () => {
    expect(propertyTitle({ ...base, neighborhood: 'Vila piloto ' })).toBe(
      'Casa 3 quartos à venda no Vila piloto, Três Lagoas',
    )
  })

  test('sem bairro e sem cidade, os trechos somem inteiros', () => {
    expect(propertyTitle({ ...base, neighborhood: null, city: null })).toBe(
      'Casa 3 quartos à venda',
    )
  })

  // Trava de regressão: o Google corta o título perto de 60 caracteres, e a
  // marca ainda é acrescentada depois pelo titleTemplate do app.vue. Se alguém
  // engordar a fórmula, este teste avisa antes de ir para produção.
  test('cabe no corte do Google', () => {
    const longo = propertyTitle({
      ...base,
      type: 'apartamento',
      bedrooms: 4,
      neighborhood: 'Jardim Alvorada',
    })
    expect(longo.length).toBeLessThanOrEqual(60)
  })
})
```

- [ ] **Step 2: Rode para ver falhar**

Run: `npx vitest run test/shared/property-title.test.ts`
Expected: FAIL com `Cannot find module '~~/shared/utils/property-title'`

- [ ] **Step 3: Implemente o mínimo**

Crie `shared/utils/property-title.ts`:

```ts
import type { PropertyType, PropertyPurpose } from '~~/shared/models/property'
import { PROPERTY_TYPE_LABELS } from '~~/shared/models/property'

/**
 * Título da página de detalhe para a busca.
 *
 * Não usa `property.title`: aquele campo é escrito pela imobiliária para uso
 * interno, e em produção rende "Terreno · Terreno" e "J.D-Alvorada". O texto
 * dela continua no <h1> e no card — só o metadado é composto.
 *
 * A medida é quartos para imóvel construído e área para terreno. Não é
 * preciosismo: sem a área, dois terrenos do mesmo bairro geram títulos
 * idênticos, o que troca um problema de SEO por outro.
 */
export interface PropertyTitleFields {
  type: PropertyType
  purpose: PropertyPurpose
  bedrooms: number
  area: number
  neighborhood?: string | null
  city?: string | null
}

/** Tira espaço das pontas e colapsa o do meio (o cadastro tem "Vila piloto "). */
function limpar(v?: string | null): string {
  return (v || '').trim().replace(/\s+/g, ' ')
}

export function propertyTitle(p: PropertyTitleFields): string {
  const medida =
    p.type === 'terreno'
      ? p.area
        ? `${Math.round(p.area)}m²`
        : ''
      : p.bedrooms
        ? `${p.bedrooms} quarto${p.bedrooms > 1 ? 's' : ''}`
        : ''

  const pretensao = p.purpose === 'aluguel' ? 'para alugar' : 'à venda'
  const bairro = limpar(p.neighborhood)
  const cidade = limpar(p.city)

  return (
    [PROPERTY_TYPE_LABELS[p.type], medida, pretensao].filter(Boolean).join(' ') +
    (bairro ? ` no ${bairro}` : '') +
    (cidade ? `, ${cidade}` : '')
  )
}
```

- [ ] **Step 4: Rode para ver passar**

Run: `npx vitest run test/shared/property-title.test.ts`
Expected: PASS, 9 testes

- [ ] **Step 5: Commit**

```bash
git add shared/utils/property-title.ts test/shared/property-title.test.ts
git commit -m "feat(seo): título do imóvel composto de campos estruturados"
```

---

### Task 3: Categoria só de pretensão

**Files:**
- Modify: `shared/utils/category.ts`
- Test: `test/shared/category.test.ts` (criar — ainda não existe)

**Interfaces:**
- Consumes: nada das tasks anteriores
- Produces: `PropertyCategory.type` passa a ser `PropertyType | null`. Assinaturas de `categorySlug`, `categoryLabel`, `parseCategorySlug`, `allCategories` e `qualifyingCategories` não mudam.

- [ ] **Step 1: Escreva o teste que falha**

Crie `test/shared/category.test.ts`:

```ts
import { describe, expect, test } from 'vitest'
import {
  categorySlug,
  categoryLabel,
  parseCategorySlug,
  allCategories,
  qualifyingCategories,
} from '~~/shared/utils/category'

describe('categoria só de pretensão', () => {
  test('slug é a pretensão sozinha', () => {
    expect(categorySlug({ type: null, purpose: 'venda' })).toBe('a-venda')
    expect(categorySlug({ type: null, purpose: 'aluguel' })).toBe('para-alugar')
  })

  test('rótulo genérico', () => {
    expect(categoryLabel({ type: null, purpose: 'venda' })).toBe('Imóveis à venda')
    expect(categoryLabel({ type: null, purpose: 'aluguel' })).toBe('Imóveis para alugar')
  })

  test('ida e volta pelo slug', () => {
    expect(parseCategorySlug('a-venda')).toEqual({ type: null, purpose: 'venda' })
    expect(parseCategorySlug('para-alugar')).toEqual({ type: null, purpose: 'aluguel' })
  })

  // O risco óbvio: "a-venda" ser confundido com o sufixo de "casas-a-venda".
  test('não colide com a categoria de tipo', () => {
    expect(parseCategorySlug('casas-a-venda')).toEqual({ type: 'casa', purpose: 'venda' })
    expect(categorySlug({ type: 'casa', purpose: 'venda' })).toBe('casas-a-venda')
  })

  test('slug inválido continua devolvendo null', () => {
    expect(parseCategorySlug('qualquer-coisa')).toBeNull()
    expect(parseCategorySlug('')).toBeNull()
  })

  test('allCategories inclui as duas de pretensão além das oito de tipo', () => {
    const todas = allCategories()
    expect(todas).toHaveLength(10)
    expect(todas.filter((c) => c.type === null)).toHaveLength(2)
  })
})

describe('qualifyingCategories com pretensão', () => {
  const casa = { type: 'casa' as const, purpose: 'venda' as const }

  test('a de pretensão conta todos os tipos daquela pretensão', () => {
    // 3 imóveis à venda, de tipos diferentes: nenhuma categoria de tipo
    // qualifica, mas "a-venda" sim.
    const itens = [
      { type: 'casa' as const, purpose: 'venda' as const },
      { type: 'apartamento' as const, purpose: 'venda' as const },
      { type: 'terreno' as const, purpose: 'venda' as const },
    ]
    const slugs = qualifyingCategories(itens).map(categorySlug)
    expect(slugs).toContain('a-venda')
    expect(slugs).not.toContain('casas-a-venda')
  })

  test('abaixo do piso não qualifica', () => {
    const slugs = qualifyingCategories([casa, casa]).map(categorySlug)
    expect(slugs).not.toContain('a-venda')
  })
})
```

- [ ] **Step 2: Rode para ver falhar**

Run: `npx vitest run test/shared/category.test.ts`
Expected: FAIL — `categorySlug` devolve `undefined-a-venda` para `type: null`, e `allCategories` tem 8

- [ ] **Step 3: Implemente o mínimo**

Em `shared/utils/category.ts`, troque a interface e as quatro funções (mantenha os mapas `TYPE_SLUGS`, `PURPOSE_SLUGS`, `TYPE_PLURAL` e `PURPOSE_LABEL` como estão):

```ts
export interface PropertyCategory {
  /** `null` = todos os tipos: /imoveis/a-venda e /imoveis/para-alugar. */
  type: PropertyType | null
  purpose: PropertyPurpose
}

export function categorySlug(c: PropertyCategory): string {
  const pretensao = PURPOSE_SLUGS[c.purpose]
  return c.type ? `${TYPE_SLUGS[c.type]}-${pretensao}` : pretensao
}

/** "Casas à venda" / "Imóveis à venda" — usado no h1, no title e no breadcrumb. */
export function categoryLabel(c: PropertyCategory): string {
  const plural = c.type ? TYPE_PLURAL[c.type] : 'Imóveis'
  return `${plural} ${PURPOSE_LABEL[c.purpose]}`
}

/** Slug -> categoria. null quando não corresponde a nenhuma combinação válida. */
export function parseCategorySlug(slug: string): PropertyCategory | null {
  for (const c of allCategories()) {
    if (categorySlug(c) === slug) return c
  }
  return null
}

/** Todas as combinações: 8 de tipo + 2 só de pretensão. */
export function allCategories(): PropertyCategory[] {
  const out: PropertyCategory[] = []
  for (const purpose of ['venda', 'aluguel'] as PropertyPurpose[]) {
    out.push({ type: null, purpose })
    for (const type of PROPERTY_TYPES) out.push({ type, purpose })
  }
  return out
}

export function qualifyingCategories(
  items: { type: PropertyType; purpose: PropertyPurpose }[],
): PropertyCategory[] {
  return allCategories().filter(
    (c) =>
      items.filter((i) => i.purpose === c.purpose && (c.type === null || i.type === c.type))
        .length >= CATEGORY_MIN_PROPERTIES,
  )
}
```

- [ ] **Step 4: Rode a suíte inteira**

Run: `npx vitest run`
Expected: PASS. A suíte inteira porque `qualifyingCategories` e `categorySlug` já são usados pelo sitemap e pela home — se algum teste existente quebrar, a mudança de assinatura vazou.

- [ ] **Step 5: Commit**

```bash
git add shared/utils/category.ts test/shared/category.test.ts
git commit -m "feat(seo): categoria só de pretensão (a-venda, para-alugar)"
```

---

### Task 4: Nova rota de detalhe

**Files:**
- Create: `app/pages/[slug]/[codigo].vue` (movendo o conteúdo de `app/pages/imovel/[code].vue`)
- Delete: `app/pages/imovel/[code].vue`
- Test: manual (a página depende de auto-imports do Nuxt; a lógica testável já está coberta pelas Tasks 1 e 2)

**Interfaces:**
- Consumes: `propertyPath`, `propertySlug` (Task 1), `propertyTitle` (Task 2)
- Produces: rota `/{slug}/{codigo}`

- [ ] **Step 1: Mova o arquivo preservando o histórico**

```bash
mkdir -p "app/pages/[slug]"
git mv "app/pages/imovel/[code].vue" "app/pages/[slug]/[codigo].vue"
rmdir app/pages/imovel 2>/dev/null || true
```

- [ ] **Step 2: Troque o parâmetro de rota**

Em `app/pages/[slug]/[codigo].vue`, a linha 10 hoje é:

```ts
const code = computed(() => String(route.params.code));
```

Troque para:

```ts
const code = computed(() => String(route.params.codigo));
```

- [ ] **Step 3: Canonical e redirect de slug desatualizado**

Ainda no mesmo arquivo, a linha do canonical hoje é:

```ts
const canonical = `${url.origin}/imovel/${p.code}`;
```

Troque por (o import vai junto, no topo do `<script setup>`):

```ts
import { propertyPath, propertySlug } from "~~/shared/utils/property-url";
```

```ts
// O primeiro segmento é decorativo e envelhece quando o imóvel é editado. Em vez
// de 404, manda para o canônico atual — link já divulgado continua valendo.
const slugCanonico = propertySlug(p);
if (String(route.params.slug) !== slugCanonico) {
  await navigateTo(propertyPath(p), { redirectCode: 301, replace: true });
}

const canonical = `${url.origin}${propertyPath(p)}`;
```

- [ ] **Step 4: Título composto**

No mesmo arquivo, o bloco `useSeoMeta` começa assim:

```ts
useSeoMeta({
  title: `${p.title} · ${PROPERTY_TYPE_LABELS[p.type]}`,
```

e o `ogTitle` é `` `${p.title} — ${priceLabel.value}` ``. Adicione o import:

```ts
import { propertyTitle } from "~~/shared/utils/property-title";
```

e troque as duas linhas por:

```ts
  title: propertyTitle(p),
```

```ts
  ogTitle: `${propertyTitle(p)} — ${priceLabel.value}`,
```

Deixe `description`, `ogDescription`, `ogImage` e `ogType` como estão.

- [ ] **Step 5: Verifique que o `<h1>` continua com o texto da imobiliária**

Run: `grep -n "p.title" "app/pages/[slug]/[codigo].vue"`
Expected: as ocorrências no `<template>` (alt de imagem, h1) continuam usando `p.title`. Só as duas linhas do `useSeoMeta` mudaram. Se `PROPERTY_TYPE_LABELS` ficou sem uso no `<script setup>`, remova o import.

- [ ] **Step 6: Typecheck**

Run: `npx nuxt typecheck 2>&1 | grep -E "^app/pages" | grep -v "h3@"`
Expected: vazio (os erros de `h3` duplicado são pré-existentes e não têm relação)

- [ ] **Step 7: Commit**

```bash
git add -A "app/pages"
git commit -m "feat(seo): detalhe do imóvel em /{slug}/{codigo}"
```

---

### Task 5: Redirects de servidor

**Files:**
- Create: `server/utils/legacy-urls.ts` (a função pura)
- Create: `server/middleware/legacy-urls.ts` (só o handler)
- Test: `test/server/legacy-urls.test.ts`

**Interfaces:**
- Consumes: `propertyPath` (Task 1), `categorySlug` (Task 3)
- Produces: `legacyRedirectFor(path: string, query: Record<string, string>, lookup: (code: string) => PropertyUrlFields | null): string | null`

> **Por que dois arquivos.** `export default defineEventHandler(...)` executa na
> hora do import, e `test/setup.ts` registra **apenas** `createError` — nenhum
> outro helper de h3 existe fora do runtime do Nuxt. Se a função pura morasse no
> mesmo arquivo do handler, o teste explodiria ao importar, antes de rodar
> qualquer asserção. Mesmo padrão já usado em `server/utils/request-ip.ts`.

- [ ] **Step 1: Escreva o teste que falha**

Crie `test/server/legacy-urls.test.ts`:

```ts
import { describe, expect, test } from 'vitest'
import { legacyRedirectFor } from '~~/server/utils/legacy-urls'

const imovel = { type: 'casa' as const, bedrooms: 3, neighborhood: 'Centro', code: 'NC-0231' }
const lookup = (code: string) => (code.toLowerCase() === 'nc-0231' ? imovel : null)

describe('legacyRedirectFor', () => {
  test('/imovel/{codigo} vai para a URL nova', () => {
    expect(legacyRedirectFor('/imovel/NC-0231', {}, lookup)).toBe('/casa-3-quartos-centro/NC-0231')
  })

  // A busca por código sempre foi case-insensitive; o redirect não pode
  // introduzir um 404 que não existia.
  test('código em caixa diferente também redireciona', () => {
    expect(legacyRedirectFor('/imovel/nc-0231', {}, lookup)).toBe('/casa-3-quartos-centro/NC-0231')
  })

  test('código com espaço, vindo percent-encoded', () => {
    const outro = { type: 'terreno' as const, bedrooms: 0, neighborhood: null, code: 'V.D 005' }
    const l = (c: string) => (c === 'V.D 005' ? outro : null)
    expect(legacyRedirectFor('/imovel/V.D%20005', {}, l)).toBe('/terreno/V.D%20005')
  })

  test('código inexistente não redireciona — segue para o 404 normal', () => {
    expect(legacyRedirectFor('/imovel/NAO-EXISTE', {}, lookup)).toBeNull()
  })

  test('?purpose=aluguel na home vai para a página de pretensão', () => {
    expect(legacyRedirectFor('/', { purpose: 'aluguel' }, lookup)).toBe('/imoveis/para-alugar')
  })

  test('?purpose=venda também, para não deixar duas URLs do mesmo conteúdo', () => {
    expect(legacyRedirectFor('/', { purpose: 'venda' }, lookup)).toBe('/imoveis/a-venda')
  })

  test('home sem query não redireciona', () => {
    expect(legacyRedirectFor('/', {}, lookup)).toBeNull()
  })

  test('purpose desconhecido é ignorado', () => {
    expect(legacyRedirectFor('/', { purpose: 'qualquer' }, lookup)).toBeNull()
  })

  test('outras rotas passam batido', () => {
    expect(legacyRedirectFor('/imoveis/a-venda', {}, lookup)).toBeNull()
    expect(legacyRedirectFor('/casa-3-quartos-centro/NC-0231', {}, lookup)).toBeNull()
  })
})
```

- [ ] **Step 2: Rode para ver falhar**

Run: `npx vitest run test/server/legacy-urls.test.ts`
Expected: FAIL com `Cannot find module '~~/server/utils/legacy-urls'`

- [ ] **Step 3: Implemente a função pura**

Crie `server/utils/legacy-urls.ts`:

```ts
import type { PropertyUrlFields } from '~~/shared/utils/property-url'
import { propertyPath } from '~~/shared/utils/property-url'
import { categorySlug } from '~~/shared/utils/category'

/**
 * Para onde uma URL antiga deve apontar. `null` = não é URL antiga, segue o
 * fluxo normal.
 *
 * Separada do handler porque `defineEventHandler` roda no import e não existe
 * fora do runtime do Nuxt: junto, o teste explodiria antes da primeira asserção.
 */
export function legacyRedirectFor(
  path: string,
  query: Record<string, string>,
  lookup: (code: string) => PropertyUrlFields | null,
): string | null {
  if (path.startsWith('/imovel/')) {
    const code = decodeURIComponent(path.slice('/imovel/'.length))
    const p = lookup(code)
    return p ? propertyPath(p) : null
  }

  if (path === '/') {
    const purpose = query.purpose
    if (purpose === 'aluguel' || purpose === 'venda') {
      return `/imoveis/${categorySlug({ type: null, purpose })}`
    }
  }

  return null
}
```

- [ ] **Step 4: Rode para ver passar**

Run: `npx vitest run test/server/legacy-urls.test.ts`
Expected: PASS, 9 testes

- [ ] **Step 5: Ligue o handler ao evento**

Crie `server/middleware/legacy-urls.ts`. Ele não tem lógica própria — só resolve
o tenant, entrega a lista e emite o 301:

```ts
import type { PropertyUrlFields } from '~~/shared/utils/property-url'
import { listActiveProperties } from '~~/server/repositories/property.repository'
import { legacyRedirectFor } from '~~/server/utils/legacy-urls'

export default defineEventHandler(async (event) => {
  if (event.method !== 'GET') return
  const path = (event.path || '').split('?')[0] || '/'
  if (!path.startsWith('/imovel/') && path !== '/') return
  if (event.context.platformRoot) return

  // A lista ativa do tenant já está cacheada — é o mesmo cache que o agents.ts
  // usa —, então resolver o código aqui não custa consulta extra.
  let lista: PropertyUrlFields[] = []
  if (path.startsWith('/imovel/')) {
    const tenant = event.context.tenant ?? (await resolveTenantForHost(getHostname(event)))
    if (!tenant) return
    lista = await cached(tenantCacheKey(tenant.id, 'properties:active'), () =>
      listActiveProperties(publicSupabase(), tenant.id),
    )
  }

  const destino = legacyRedirectFor(path, getQuery(event) as Record<string, string>, (code) =>
    lista.find((p) => p.code.toLowerCase() === code.toLowerCase()) ?? null,
  )
  if (destino) return sendRedirect(event, destino, 301)
})
```

- [ ] **Step 6: Confirme que a suíte segue verde**

Run: `npx vitest run`
Expected: PASS. O middleware não é importado por teste nenhum — se algum quebrar aqui, é sinal de que a função pura ficou no arquivo errado.

- [ ] **Step 7: Commit**

```bash
git add server/utils/legacy-urls.ts server/middleware/legacy-urls.ts test/server/legacy-urls.test.ts
git commit -m "feat(seo): 301 das URLs antigas de imóvel e de ?purpose="
```

---

### Task 6: Os oito chamadores usam o helper

**Files:**
- Modify: `app/components/PropertyCard.vue:45,67`
- Modify: `app/pages/admin/leads/index.vue:679`
- Modify: `server/repositories/lead.repository.ts:18` (o lead precisa carregar mais campos do imóvel — ver Step 3)
- Modify: `shared/models/lead.ts:119`
- Modify: `server/mappers/lead.mapper.ts:10,30`
- Modify: `app/plugins/webmcp.client.ts:57`
- Modify: `server/routes/sitemap.xml.get.ts:39`
- Modify: `server/routes/feed/imoveis.xml.get.ts:125`
- Modify: `server/utils/markdown.ts:54,89,118`
- Modify: `server/middleware/agents.ts:42`
- Test: `test/server/property-url-callers.test.ts`

**Interfaces:**
- Consumes: `propertyPath`, `propertySlug` (Task 1)
- Produces: nada novo

- [ ] **Step 1: Escreva o teste que falha**

Este é o teste que impede os chamadores de divergirem de novo. Crie `test/server/property-url-callers.test.ts`:

```ts
import { readFileSync } from 'node:fs'
import { join } from 'node:path'
import { describe, expect, test } from 'vitest'

/**
 * A URL do imóvel era montada à mão em oito lugares, e bastava um deles ficar
 * para trás numa mudança de formato para o sitemap publicar link que dá 404.
 * Este teste falha se alguém voltar a interpolar o caminho na unha.
 */
const ARQUIVOS = [
  'app/components/PropertyCard.vue',
  'app/pages/admin/leads/index.vue',
  'app/plugins/webmcp.client.ts',
  'server/routes/sitemap.xml.get.ts',
  'server/routes/feed/imoveis.xml.get.ts',
  'server/utils/markdown.ts',
  'server/middleware/agents.ts',
]

describe('caminho do imóvel vem sempre do helper', () => {
  for (const arquivo of ARQUIVOS) {
    test(`${arquivo} não monta /imovel/ na mão`, () => {
      const fonte = readFileSync(join(process.cwd(), arquivo), 'utf8')
      const semComentario = fonte.replace(/\/\*[\s\S]*?\*\//g, '').replace(/^\s*\/\/.*$/gm, '')
      expect(semComentario).not.toMatch(/['"`]\/imovel\//)
    })
  }
})
```

- [ ] **Step 2: Rode para ver falhar**

Run: `npx vitest run test/server/property-url-callers.test.ts`
Expected: FAIL em todos os sete arquivos

- [ ] **Step 3: Troque chamador por chamador**

Em cada arquivo, importe o helper e substitua a interpolação. Nos arquivos de `app/pages` e `app/components` use aspas duplas e ponto-e-vírgula; em `server/` use aspas simples e sem ponto-e-vírgula.

`app/components/PropertyCard.vue` — adicione ao `<script setup>`:

```ts
import { propertyPath } from "~~/shared/utils/property-url";
```

e troque as duas ocorrências de `` :to="`/imovel/${property.code}`" `` por `:to="propertyPath(property)"`.

`app/pages/admin/leads/index.vue` — este exige mais que uma troca. Hoje o lead
carrega só `{ code, title }` do imóvel (`select('*, properties(code, title)')`),
e `propertyPath` precisa de `type`, `bedrooms` e `neighborhood` — passar o que
existe hoje **não compila**. Três arquivos mudam junto:

1. `server/repositories/lead.repository.ts`, no `listLeads`:

```ts
    .select('*, properties(code, title, type, bedrooms, neighborhood)')
```

2. `shared/models/lead.ts`, na linha 119:

```ts
  property?: {
    code: string
    title: string
    type: PropertyType
    bedrooms: number
    neighborhood: string | null
  } | null
```

com `import type { PropertyType } from '~~/shared/models/property'` no topo, se
ainda não houver.

3. `server/mappers/lead.mapper.ts`, na linha 30:

```ts
    property: property
      ? {
          code: property.code,
          title: property.title,
          type: property.type,
          bedrooms: property.bedrooms,
          neighborhood: property.neighborhood,
        }
      : null,
```

Ajuste `LeadPropertyFields` no mesmo arquivo para incluir os três campos novos.

Só então, em `app/pages/admin/leads/index.vue`, troque
`` :to="`/imovel/${l.property.code}`" `` por `:to="propertyPath(l.property)"` com
o import de `~~/shared/utils/property-url`.

`app/plugins/webmcp.client.ts` — troque `` `${origin}/imovel/${encodeURIComponent(p.code)}` `` por `` `${origin}${propertyPath(p)}` ``.

`server/routes/sitemap.xml.get.ts` — troque `` `${origin}/imovel/${encodeURIComponent(p.code)}` `` por `` `${origin}${propertyPath(p)}` ``.

`server/routes/feed/imoveis.xml.get.ts` — troque `` origin + '/imovel/' + encodeURIComponent(p.code) `` por `` origin + propertyPath(p) ``.

`server/utils/markdown.ts` — as três ocorrências de `` `${origin}/imovel/${encodeURIComponent(p.code)}` `` viram `` `${origin}${propertyPath(p)}` ``.

`server/middleware/agents.ts` — a linha 13 continua detectando a rota, mas o caminho mudou. Troque:

```ts
const isProperty = path.startsWith('/imovel/')
```

por:

```ts
// Duas barras e o último segmento sendo um código conhecido: é detalhe de
// imóvel. A lista é consultada logo abaixo, então a checagem final é lá.
const isProperty = /^\/[^/]+\/[^/]+$/.test(path) && !path.startsWith('/imoveis/')
```

e o bloco que fatiava o prefixo:

```ts
const code = decodeURIComponent(path.slice('/imovel/'.length))
const property = list.find((p) => p.code.toLowerCase() === code.toLowerCase())
```

por:

```ts
const code = decodeURIComponent(path.slice(path.lastIndexOf('/') + 1))
const property = list.find((p) => p.code.toLowerCase() === code.toLowerCase())
```

- [ ] **Step 4: Rode a suíte inteira**

Run: `npx vitest run`
Expected: PASS. A suíte inteira porque `markdown.ts` e o sitemap já têm testes que fixam a saída — se o formato mudou, eles apontam onde.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "refactor(seo): oito chamadores passam a usar propertyPath"
```

---

### Task 7: Página de categoria aceita pretensão

**Files:**
- Modify: `app/pages/imoveis/[categoria].vue`
- Test: manual (a página depende de auto-imports do Nuxt)

**Interfaces:**
- Consumes: `parseCategorySlug`, `categoryLabel`, `categorySlug`, `CATEGORY_MIN_PROPERTIES` (Task 3)
- Produces: rotas `/imoveis/a-venda` e `/imoveis/para-alugar`

- [ ] **Step 1: Filtro deixa de exigir tipo**

Em `app/pages/imoveis/[categoria].vue`, o `inCategory` hoje é:

```ts
const inCategory = computed(() =>
  (properties.value ?? []).filter((p) => p.type === category.type && p.purpose === category.purpose),
)
```

Troque por:

```ts
const inCategory = computed(() =>
  (properties.value ?? []).filter(
    (p) => p.purpose === category.purpose && (category.type === null || p.type === category.type),
  ),
)
```

E logo abaixo, `filters.purpose = category.purpose` continua; acrescente, para que o seletor de tipo da tela não venha travado na página de pretensão:

```ts
if (category.type) filters.type = category.type
```

- [ ] **Step 2: Piso vira noindex nas páginas de pretensão**

O bloco atual é:

```ts
if (inCategory.value.length < CATEGORY_MIN_PROPERTIES) {
  throw createError({ statusCode: 404, statusMessage: 'Categoria sem imóveis suficientes.' })
}
```

Troque por:

```ts
// As páginas de pretensão ficam linkadas na home, então 404 apareceria na cara
// do visitante. Elas respondem 200 e saem do índice; as de tipo+pretensão,
// que ninguém alcança pelo menu, seguem com 404.
const abaixoDoPiso = computed(() => inCategory.value.length < CATEGORY_MIN_PROPERTIES)
if (abaixoDoPiso.value && category.type !== null) {
  throw createError({ statusCode: 404, statusMessage: 'Categoria sem imóveis suficientes.' })
}

useHead(() => ({
  meta: abaixoDoPiso.value ? [{ name: 'robots', content: 'noindex,follow' }] : [],
}))
```

- [ ] **Step 3: Estado vazio com convite ao contato**

No `<template>`, onde hoje a lista é renderizada, acrescente antes do grid:

```vue
<div v-if="!filtered.length" class="cat-vazio">
  <p>
    Ainda não temos {{ categoryLabel(category).toLowerCase() }}{{ cityLabel }} publicados no
    momento.
  </p>
  <a class="btn-wa" :href="whatsappLink()" target="_blank" rel="noopener">
    Avise-me quando aparecer
  </a>
</div>
```

e no `<script setup>`, se `useContact` ainda não estiver importado:

```ts
const { whatsappLink } = useContact()
```

- [ ] **Step 4: Verifique as duas rotas no navegador**

Suba o preview e confira, com o tenant `demo` (3 aluguéis, indexa) e o `tatiane` (0 aluguéis, noindex):

```
http://localhost:3000/imoveis/a-venda?tenant=demo
http://localhost:3000/imoveis/para-alugar?tenant=demo
http://localhost:3000/imoveis/para-alugar?tenant=tatiane
```

Expected: as duas primeiras listam imóveis e não têm `robots`; a terceira responde 200 com o estado vazio e traz `<meta name="robots" content="noindex,follow">`.

- [ ] **Step 5: Commit**

```bash
git add "app/pages/imoveis/[categoria].vue"
git commit -m "feat(seo): /imoveis/a-venda e /imoveis/para-alugar"
```

---

### Task 8: Home volta a ser vitrine

**Files:**
- Modify: `app/pages/index.vue`
- Test: manual

**Interfaces:**
- Consumes: `categorySlug`, `categoryLabel`, `qualifyingCategories`, `allCategories` (Task 3)
- Produces: nada novo

- [ ] **Step 1: Tire a pretensão da URL**

Em `app/pages/index.vue`, remova o bloco que lê a query e sincroniza o filtro:

```ts
const route = useRoute();
const isRent = computed(() => route.query.purpose === "aluguel");
watchEffect(() => {
  const next = isRent.value ? "aluguel" : "venda";
  if (filters.purpose !== next) {
    filters.purpose = next;
    filters.maxPrice = 0;
  }
});
```

O filtro de pretensão continua existindo na tela — só deixa de viver na URL, porque `/imoveis/a-venda` e `/imoveis/para-alugar` passaram a ser as páginas canônicas de cada pretensão. Mantenha `const route = useRoute();` se ele for usado em outro ponto do arquivo.

- [ ] **Step 2: Título e canonical deixam de depender da query**

O `useSeoMeta` condicional vira:

```ts
useSeoMeta({
  title: () => tenant.value?.heroTitle || "Imóveis à venda e para alugar",
  ogTitle: () =>
    `${tenant.value?.name || "Imóveis"} · Imóveis à venda e para alugar`,
});
```

e o `useHead` do canonical vira:

```ts
useHead(() => ({
  link: [{ rel: "canonical", href: requestUrl.origin + "/" }],
}));
```

- [ ] **Step 3: As duas páginas de pretensão entram nos links internos**

O `catLinks` hoje lista só as categorias qualificadas. As de pretensão precisam aparecer sempre, inclusive quando desindexadas — `noindex,follow` tira do índice, não da navegação:

```ts
const catLinks = computed(() => {
  const pretensoes = allCategories()
    .filter((c) => c.type === null)
    .map((c) => ({
      href: `/imoveis/${categorySlug(c)}`,
      label: `${categoryLabel(c)}${tenant.value?.city ? " em " + tenant.value.city : ""}`,
    }));
  const tipos = qualifyingCategories(list.value)
    .filter((c) => c.type !== null)
    .map((c) => ({
      href: `/imoveis/${categorySlug(c)}`,
      label: `${categoryLabel(c)}${tenant.value?.city ? " em " + tenant.value.city : ""}`,
    }));
  return [...pretensoes, ...tipos];
});
```

Acrescente `allCategories` ao import existente de `~~/shared/utils/category`.

- [ ] **Step 4: Confira o redirect de ponta a ponta**

Com o preview no ar:

```bash
curl -s -o /dev/null -w "%{http_code} -> %{redirect_url}\n" -H "Cookie: dev_tenant=demo" "http://localhost:3000/?purpose=aluguel"
```

Expected: `301 -> /imoveis/para-alugar`

- [ ] **Step 5: Commit**

```bash
git add app/pages/index.vue
git commit -m "feat(seo): home vira vitrine, pretensão sai da query"
```

---

### Task 9: Sitemap com as páginas de pretensão

**Files:**
- Modify: `server/routes/sitemap.xml.get.ts:27-28`
- Test: `test/server/sitemap-categorias.test.ts`

**Interfaces:**
- Consumes: `qualifyingCategories`, `categorySlug` (Task 3)
- Produces: nada novo

- [ ] **Step 1: Escreva o teste que falha**

Crie `test/server/sitemap-categorias.test.ts`:

```ts
import { describe, expect, test } from 'vitest'
import { qualifyingCategories, categorySlug } from '~~/shared/utils/category'

/**
 * O sitemap não pode publicar URL que responde noindex: seria pedir indexação
 * de uma página que se recusa a ser indexada. O piso decide os dois.
 */
describe('categorias no sitemap', () => {
  const venda = { type: 'casa' as const, purpose: 'venda' as const }

  test('pretensão com estoque suficiente entra', () => {
    const slugs = qualifyingCategories([venda, venda, venda]).map(categorySlug)
    expect(slugs).toContain('a-venda')
    expect(slugs).toContain('casas-a-venda')
  })

  test('pretensão sem estoque fica fora', () => {
    const slugs = qualifyingCategories([venda, venda, venda]).map(categorySlug)
    expect(slugs).not.toContain('para-alugar')
  })

  // O caso da tatiane e da olmi: a página existe e está linkada na home, mas
  // responde noindex — logo, não pode estar aqui.
  test('um único aluguel não coloca a página no sitemap', () => {
    const itens = [venda, venda, venda, { type: 'casa' as const, purpose: 'aluguel' as const }]
    const slugs = qualifyingCategories(itens).map(categorySlug)
    expect(slugs).not.toContain('para-alugar')
  })
})
```

- [ ] **Step 2: Rode para ver falhar ou passar**

Run: `npx vitest run test/server/sitemap-categorias.test.ts`
Expected: PASS já na primeira execução, porque a Task 3 fez `qualifyingCategories` incluir as de pretensão. Este teste é trava de regressão, não guia de implementação — se ele falhar aqui, a Task 3 saiu errada e é lá que se corrige.

- [ ] **Step 3: Confirme que o sitemap não precisa mudar**

Run: `sed -n '25,32p' server/routes/sitemap.xml.get.ts`
Expected: o `map` sobre `qualifyingCategories(list)` já monta `/imoveis/${categorySlug(c)}` sem olhar para `c.type`. Como a Task 3 mudou a fonte, as páginas de pretensão entram sozinhas. Não altere o arquivo.

- [ ] **Step 4: Verifique o sitemap gerado**

Com o preview no ar:

```bash
curl -s -H "Cookie: dev_tenant=demo" http://localhost:3000/sitemap.xml | grep -o "<loc>[^<]*</loc>" | head -20
```

Expected: aparecem `/imoveis/a-venda` e `/imoveis/para-alugar` (demo tem 9 vendas e 3 aluguéis), e as URLs de imóvel estão no formato `/{slug}/{codigo}`.

Repita com `dev_tenant=tatiane` e confirme que `/imoveis/para-alugar` **não** aparece.

- [ ] **Step 5: Commit**

```bash
git add test/server/sitemap-categorias.test.ts
git commit -m "test(seo): trava o piso que decide sitemap e noindex"
```

---

### Task 10: Verificação de ponta a ponta

**Files:** nenhum (só verificação)

- [ ] **Step 1: Suíte e typecheck**

Run: `npx vitest run && npx nuxt typecheck 2>&1 | grep -E "^(app|server|shared|test)/" | grep -vE "h3@1\.15\.11|tenant\.mapper"`
Expected: todos os testes passam; o typecheck não imprime nada além do ruído pré-existente já filtrado.

- [ ] **Step 2: Os redirects respondem 301**

```bash
curl -s -o /dev/null -w "imovel antigo: %{http_code} -> %{redirect_url}\n" -H "Cookie: dev_tenant=demo" "http://localhost:3000/imovel/NC-0339"
curl -s -o /dev/null -w "purpose:       %{http_code} -> %{redirect_url}\n" -H "Cookie: dev_tenant=demo" "http://localhost:3000/?purpose=aluguel"
```

Expected: o primeiro 301 para `/{slug}/NC-0339`; o segundo 301 para `/imoveis/para-alugar`.

- [ ] **Step 3: A página nova responde e se auto-canonicaliza**

```bash
curl -s -H "Cookie: dev_tenant=demo" "http://localhost:3000/$(curl -s -H 'Cookie: dev_tenant=demo' http://localhost:3000/api/properties | node -e "let s='';process.stdin.on('data',c=>s+=c).on('end',()=>{const p=JSON.parse(s)[0];process.stdout.write('x')})")" > /dev/null
curl -s -o /dev/null -w "slug errado: %{http_code} -> %{redirect_url}\n" -H "Cookie: dev_tenant=demo" "http://localhost:3000/slug-totalmente-errado/NC-0339"
```

Expected: o slug errado responde 301 para o caminho canônico.

- [ ] **Step 4: O feed XML saiu com o formato novo**

```bash
curl -s -H "Cookie: dev_tenant=demo" http://localhost:3000/feed/imoveis.xml | grep -o "<DetailViewUrl>[^<]*" | head -3
```

Expected: URLs no formato `/{slug}/{codigo}`, nenhuma com `/imovel/`.

- [ ] **Step 5: Nada de `/imovel/` sobrou no código**

Run: `grep -rn "'/imovel/\|\"/imovel/\|\`/imovel/" app/ server/ shared/ --include=*.ts --include=*.vue`
Expected: nenhuma ocorrência fora de comentário. As que restarem em comentário devem descrever o formato ANTIGO e estar corretas no contexto.

- [ ] **Step 6: Commit final**

```bash
git commit --allow-empty -m "chore(seo): verificação de ponta a ponta das URLs novas"
```
